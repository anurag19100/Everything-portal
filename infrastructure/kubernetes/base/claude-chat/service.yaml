apiVersion: v1
kind: Service
metadata:
  name: claude-chat
  labels:
    app: claude-chat
    service: claude-chat
spec:
  ports:
  - port: 3000
    targetPort: 3000
    protocol: TCP
    name: http
  selector:
    app: claude-chat
  type: ClusterIP
